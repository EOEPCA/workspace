apiVersion: apiextensions.crossplane.io/v1
kind: Composition
metadata:
  name: storage
spec:
  compositeTypeRef:
    apiVersion: epca.eo/v1beta1
    kind: XStorage
  mode: Pipeline
  pipeline:
    - step: setup-storage
      functionRef:
        name: function-go-templating
      input:
        apiVersion: gotemplating.fn.crossplane.io/v1beta1
        kind: GoTemplate
        source: Inline
        inline:
          template: |
            {{- $xr := .observed.composite.resource }}
            {{- if $xr.metadata.namespace }}
            {{- range $i, $bucket := $xr.spec.buckets }}
            ---
            apiVersion: minio.crossplane.io/v1
            kind: Bucket
            metadata:
              name: {{ $bucket.name }}
              annotations:
                gotemplating.fn.crossplane.io/composition-resource-name: bucket-{{ $i }}
                gotemplating.fn.crossplane.io/ready: "True"
            spec:
              forProvider:
                region: ""
              providerConfigRef:
                name: provider-minio
            {{- range $j, $rw := $bucket.accessReadWrite }}
            ---
            apiVersion: minio.crossplane.io/v1
            kind: Policy
            metadata:
              name: {{ $rw }}.readwrite.{{ $bucket.name }}
              annotations:
                gotemplating.fn.crossplane.io/composition-resource-name: policy-rw-{{ $i }}-{{ $j }}
                gotemplating.fn.crossplane.io/ready: "True"
            spec:
              forProvider:
                rawPolicy: |
                  {
                    "Version": "2012-10-17",
                    "Statement": [
                      {
                        "Effect": "Allow",
                        "Action": [
                          "s3:GetBucketLocation",
                          "s3:ListBucket",
                          "s3:GetObject",
                          "s3:PutObject",
                          "s3:DeleteObject"
                        ],
                        "Resource": [
                          "arn:aws:s3:::{{ $bucket.name }}",
                          "arn:aws:s3:::{{ $bucket.name }}/*"
                        ]
                      }
                    ]
                  }
              providerConfigRef:
                name: provider-minio
            {{- end }}
            {{- range $j, $ro := $bucket.accessReadOnly }}
            ---
            apiVersion: minio.crossplane.io/v1
            kind: Policy
            metadata:
              name: {{ $ro }}.readonly.{{ $bucket.name }}
              annotations:
                gotemplating.fn.crossplane.io/composition-resource-name: policy-ro-{{ $i }}-{{ $j }}
                gotemplating.fn.crossplane.io/ready: "True"
            spec:
              forProvider:
                rawPolicy: |
                  {
                    "Version": "2012-10-17",
                    "Statement": [
                      {
                        "Effect": "Allow",
                        "Action": [
                          "s3:GetBucketLocation",
                          "s3:ListBucket"
                        ],
                        "Resource": [
                          "arn:aws:s3:::{{ $bucket.name }}"
                        ]
                      },
                      {
                        "Effect": "Allow",
                        "Action": [
                          "s3:GetObject"
                        ],
                        "Resource": [
                          "arn:aws:s3:::{{ $bucket.name }}/*"
                        ]
                      }
                    ]
                  }
              providerConfigRef:
                name: provider-minio
            {{- end }}
            {{- end }}
            ---
            apiVersion: minio.crossplane.io/v1
            kind: User
            metadata:
              name: {{ $xr.metadata.namespace }}
              annotations:
                gotemplating.fn.crossplane.io/composition-resource-name: user-{{ $xr.metadata.namespace }}
                gotemplating.fn.crossplane.io/ready: "True"
            spec:
              forProvider:
                userName: {{ $xr.metadata.namespace }}
                policies:
                {{- range $i, $bucket := $xr.spec.buckets }}
                  {{- if has $xr.metadata.namespace $bucket.accessReadWrite }}
                  - {{ $xr.metadata.namespace }}.readwrite.{{ $bucket.name }}
                  {{- end }}
                  {{- if has $xr.metadata.namespace $bucket.accessReadOnly }}
                  - {{ $xr.metadata.namespace }}.readonly.{{ $bucket.name }}
                  {{- end }}
                {{- end }}
              providerConfigRef:
                name: provider-minio
              writeConnectionSecretToRef:
                name: bucket-{{ $xr.metadata.namespace }}
                namespace: {{ $xr.metadata.namespace }}
            {{- end }}
